/** Brand tokens — same hex values as apps/native/tailwind.config.js.
 * (NativeWind classes don't run in Snack, so the mirror uses StyleSheet
 * with these constants instead of `className`.) */
export const colors = {
  primary: "#87e1cc",
  primaryDark: "#1a7a6e",
  primaryLight: "#d4f4ed",
  muted: "#6b7280",
  border: "#e5e7eb",
  surface: "#ffffff",
  background: "#f7f8fa",
  foreground: "#0a0a0a",
  white: "#ffffff",
  placeholder: "#9ca3af",
  amber: "#f59e0b",
  redBg: "#fef2f2",
  redIcon: "#b91c1c",
  redText: "#b91c1c",
};

/** border-radius "card" token (16px) from the native tailwind config. */
export const radiusCard = 16;
